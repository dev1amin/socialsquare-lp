import { HeroGeometric } from "@/components/ui/shape-landing-hero";

const SSHeroSection = () => {
  return (
    <section id="hero">
      <HeroGeometric />
    </section>
  );
};

export default SSHeroSection;
